import * as React from "react"
import type { PageProps } from "gatsby"

const Columns: React.FC<PageProps> = () => {

  return (
    <div className="flex w-full h-[300]">
    </div>
  );
}

export default Columns
